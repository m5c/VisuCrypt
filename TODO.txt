Fix balck white inversion bug
Store output image in same dir as input image
Generate printable PDF with maximized png images in addition to individual files.
One Time Pad generator is wasteful, creates random byte but then only takes first position as random value.
